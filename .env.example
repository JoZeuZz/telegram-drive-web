# Global environment contract for telegram-drive-web
# Single source of truth for local dev, Coolify UI, and systemd deployments.
#
# Usage examples:
# - Local backend: cp .env.example server/.env
# - Local Coolify compose validation: cp .env.example .env.coolify
# - Coolify production: set real values in Coolify UI (do not commit secrets)

# Server
APP_ENV=development
HOST=127.0.0.1
PORT=8080

# Frontend origin / CORS
# In production set to your public URL, e.g. https://drive.example.com
FRONTEND_PORT=3000
CORS_ALLOWED_ORIGIN=http://localhost:3000

# Paths
# For local dev these are relative to server/ when running cargo there.
DATA_DIR=./data
CACHE_DIR=./data/cache

# Security
# Generate with: openssl rand -hex 32
# REQUIRED in production
SESSION_SECRET=CHANGE_ME_random_hex_string
COOKIE_SECURE=false
SESSION_TTL_HOURS=8
# Maximum upload size per file in bytes (default: ~2 GB, decimal)
MAX_FILE_SIZE_BYTES=2097152000
# Premium upload size per file in bytes (default: 4 GiB)
PREMIUM_MAX_FILE_SIZE_BYTES=4294967296

# Dynamic limits and fallback behavior
DYNAMIC_LIMITS_ENABLED=true
FALLBACK_TO_FREE_ON_ERROR=true
PREMIUM_DETECTION_TTL_SECS=3600

# Structured folders feature toggle
FORUMS_ENABLED=true

# Daily bandwidth limits per tier (bytes)
FREE_DAILY_BANDWIDTH_LIMIT_BYTES=268435456000
PREMIUM_DAILY_BANDWIDTH_LIMIT_BYTES=858993459200

ADMIN_PASSWORD=changeme
TRUST_PROXY_HEADERS=false

# Rate limiting
APP_AUTH_RATE_LIMIT_MAX_REQUESTS=10
APP_AUTH_RATE_LIMIT_WINDOW_SECS=60
TELEGRAM_AUTH_RATE_LIMIT_MAX_REQUESTS=5
TELEGRAM_AUTH_RATE_LIMIT_WINDOW_SECS=60

# Telegram credentials (from https://my.telegram.org/apps)
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=replace-with-api-hash

# Logging
RUST_LOG=info
LOG_FORMAT=json
